require('particleground')

  ; (function () {
    $(document).ready(function () {
      $('section.banner').particleground({
        dotColor: '#0078ff',
        lineColor: '#0078ff'
      });
      $('.f-2 .hero').particleground({
        dotColor: '#0078ff',
        lineColor: '#0078ff'
      });
    })
  })()